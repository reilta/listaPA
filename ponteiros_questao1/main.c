#include <stdio.h>

int main()
{
    /* até o momento ponteiros foi muito util quando tratamos de alocação de "espaço"
     * de memoria do computador, ajudando o pragrama ser mais rapido e agil, além
     * de consumir menos memoria ram do computador, dependendo da forma que for utilizado. */
    return 0;
}
